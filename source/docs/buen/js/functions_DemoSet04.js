/************/
function	mouseDown_ds04_Control01( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
//	document.getElementById("button_DemoSet01").src = "images/buttons/circular/90x90_Orange_off.png";
	/**************************************************************/
}




/************/
function	mouseDown_ds04_Control02( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
	/**************************************************************/
}




/************/
function	mouseDown_ds04_Control03( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
	/**************************************************************/
}




/************/
function	mouseDown_ds04_Control04( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
	/**************************************************************/
}




/************/
function	mouseDown_ds04_Control05( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
	/**************************************************************/
}




/************/
function	mouseDown_ds04_Control06( clickedControl )
{
	/***  01. Check if we're even supposed to process user events.  ***/
	if ( g_ProcessUserEvents == false )
	{
		return;		//	MUST RETURN HERE
	}
	/******************************************************************/
	
	
	/***  02. Perform the corresponding action of the control.  ***/
	/**************************************************************/
}